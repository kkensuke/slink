# slink

A managed symbolic-link CLI for macOS.

Create links, keep a hand-editable TOML registry, and inspect or restore managed links.

Implementation is being developed incrementally on `feat/slink-cli`.
