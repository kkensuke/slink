# slink 初版設計 — macOS

ユーザーが選択した機能と方針を「確定」とし、具体的な処理方法・終了コード等は実装に向けた提案として区別する。初版の対応OSは macOS。実装言語は Rust を第一候補とする提案であり、リポジトリ・最低対応macOSバージョン・配布方法は未指定。本書は設計成果物であり、slink 本体の実装やmacOS実機検証はまだ行っていない。

## 確定した方針

| 項目 | 決定 |
| --- | --- |
| 管理単位 | 通常はユーザー共通の管理ファイル1つ。必要なときだけ別ファイルを明示指定する |
| 手編集による項目削除 | 管理だけ解除し、実際のリンクは残す。実際の削除には remove を使う |
| 登録内容と異なるターゲット | 通常の fix では報告だけ。明示的なオプションを指定したときに張り直す |
| 管理ファイル形式 | TOML。`[[links]]` に `link` / `target` を記述する |
| 初版の補助機能 | --dry-run、--relative、remove --keep-link、--parents を採用する |
| 存在しないターゲット | 作成・fix ともリンク作成を許可し、ターゲットが存在しないことを報告する |
| 対応OS | 初版は macOS |

--json は今回の選択に含まれなかったため、初版の対象から外す。

この決定から、過去の登録項目を保持して削除対象を推測する適用履歴は、通常の管理機能には必要ない。障害復旧や同時編集への対処とは別の論点として扱う。

## 目的と責務の境界（提案）

任意のシンボリックリンクを作成・登録し、後から登録内容との一致と到達可能性を確認できる小さなCLIにする。

管理ファイルには期待するリンクを記述する。各コマンドは「登録情報を変える」「実際のリンクを変える」「検査対象を集める」の組み合わせとして実装し、同じ検査・変更処理を複数コマンドに重複実装しない。

| コマンド | 責務 | 管理ファイルの変更 | リンクの変更 |
| --- | --- | --- | --- |
| `slink <target> <link>` | 新しいリンクの作成と登録 | 追加 | 作成 |
| `slink list` | 登録内容の表示 | なし | なし |
| `slink check [link…]` | 登録内容と実態の比較・診断 | なし | なし |
| `slink fix [link…]` | 登録内容に基づく復元 | なし | 条件付き |
| `slink remove <link…>` | リンク削除と登録解除 | 削除 | 条件付き |
| `slink adopt <link…>` | 既存リンクの登録 | 追加 | なし |
| `slink scan <directory…>` | 指定範囲のリンク探索 | なし | なし |

- list は登録内容だけを表示し、リンクの状態検査はしない。
- check と fix はリンク指定がなければ選択中の管理ファイルの全項目を対象にする。
- remove と adopt は対象の明示を必須とする。
- scan はディレクトリを明示指定し、配下を再帰探索する。探索中に見つけたディレクトリへのシンボリックリンクは辿らない。探索ルート自体がシンボリックリンクの場合は初版では拒否する案。
- scan は選択中の管理ファイルに登録されているかを表示できる。別の管理ファイルまで探索して所有者を推測しない。
- scan と check のリンク検査処理は共有する。
- サブコマンド名と同名のターゲットは `slink -- list ./list-link` のように指定できるようにする。`--` の後は作成用の2引数として解釈する。
- `<link>` は作成する名前そのもの。既存ディレクトリが渡されたときに、その中へターゲットの名前で作成する省略動作は追加しない案。

## 管理ファイルの選択（提案）

通常のファイル名は `~/.config/slink/links.toml`。macOSでも、このCLIの規約としてXDG形式を採用する案。絶対パスの `XDG_CONFIG_HOME` が設定されている場合は、その配下の `slink/links.toml` を使う。未設定・空・相対パスの場合はホーム配下の既定値を使う。これはmacOS全体の設定ファイル配置規則を述べたものではない。[XDG Base Directory Specification](https://specifications.freedesktop.org/basedir/latest/)

```sh
slink check
slink --file ./project-links.toml check
slink --file ./project-links.toml fix
```

1回の実行が扱う管理ファイルは1つ。明示指定したファイルを共通ファイルとマージしない。作業ディレクトリや親ディレクトリから管理ファイルを自動探索しない。

ファイルがない場合、作成・adopt は新しい管理ファイルを作れる。list/check/fix/remove は見つからない旨を報告する案。scan は共通ファイルがなければ登録0件として探索できるようにする。ただし --file で明示指定したファイルがない場合や、存在する管理ファイルを読めない・解析できない場合はエラーにする。読み取り系コマンドが管理ファイルを自動作成することはない。

## 管理ファイル形式（TOML採用は確定）

```toml
version = 1

# シェル設定
[[links]]
link = "~/.zshrc"
target = ".dotfiles/zsh/.zshrc"

# エディタ設定
[[links]]
link = "~/.config/nvim"
target = "../dotfiles/nvim"
```

TOMLはコメントとテーブルの配列を表現できる。[TOML仕様](https://toml.io/en/v1.0.0)

形式は1種類に絞る。YAML等と同時対応すると、コメントを保つ更新処理・検証・ドキュメントを複数用意する必要がある。

手編集のための要件案：

- 必須情報は `link` と `target`。状態・最終チェック日時・作成日時は保存しない。
- ツールの書き戻しでコメント・並び順・変更しない箇所の書式を保持する。通常の解析後の全面再出力だけでは、この要件を満たしたことにしない。
- 同じ配置先の重複登録はエラー。同じターゲットへの複数リンクは許可する。
- 未対応の version、不明な項目、型違い、構文エラーは変更前に報告する。
- 管理ファイルの更新は一時ファイルと置換を使い、途中まで書いたファイルを正規ファイルとして残さない。
- 同じ管理ファイルに対する slink 同士の更新を排他し、読み込み後の手編集を保存直前にも検査する。検出した競合では更新を中止する。協調しないエディタの同時書き込みを完全に排除する保証はしない。詳細は「保存と途中失敗」を参照。

### パスの意味（提案）

| 入力 | 解釈 |
| --- | --- |
| CLIの相対 link | 呼び出し時の作業ディレクトリ基準 |
| 管理ファイル内の相対 link | 管理ファイルのあるディレクトリ基準 |
| 管理ファイル内の link の先頭 `~/` | 実行ユーザーのホームディレクトリに展開 |
| 管理ファイル内の target | シンボリックリンクに格納する文字列そのもの |
| --relative なしのCLI target | 格納する文字列そのもの。相対表記はリンクの親ディレクトリ基準 |
| --relative ありのCLI target | 実行ディレクトリ基準のパスとして受け取り、リンクの親から参照する相対表記を生成 |

CLIで受け取った配置先は、再実行する作業ディレクトリで意味が変わらない形で保存する。初版では自動登録する link を絶対パスにする案。手編集した link の表記は、無関係な更新で変えない。

管理ファイルの target と --relative なしのCLI target は相対・絶対の表記を保持する。相対 target はリンクが置かれるディレクトリ基準で解決される。管理ファイル内の target に対してホーム展開、環境変数展開、シェル評価は行わない。[symlink(2)](https://man7.org/linux/man-pages/man2/symlink.2.html)

CLIの `~` や環境変数は、通常は slink に渡される前にシェルが展開する。管理ファイルの文字列とは区別して説明する。

adopt は readlink で得られたターゲットをそのまま記録する。一致判定も格納文字列を基準とし、同じ最終ファイルに到達するという理由で相対表記・絶対表記・別のシンボリックリンク経由の表記を同一扱いしない。

配置先の重複検証では、最終要素のシンボリックリンクを辿ってターゲットを配置先と混同しない。途中の symlink と `..` を含むパスを、意味が変わる字句整理で比較しない。macOSでの識別方法は下記の方針を用いる。

## 手編集の反映

| 編集 | 結果 |
| --- | --- |
| 項目を追加 | check で検査でき、fix で不足するリンクを作成できる |
| target を変更 | 通常の fix は不一致を報告。明示オプションで張り直す |
| 項目を削除 | 実リンクを残したまま管理解除（確定） |
| link の値を変更 | 旧配置先の管理解除と、新配置先の登録になる。旧リンクは残る |

項目の削除や link の変更を、自動の削除・移動と解釈しない。

## 検査と修復（具体的な表記は提案）

状態は独立した軸で記録し、複数の事象を1つの状態名に詰め込まない。

| 診断するもの | 状態 |
| --- | --- |
| 配置先と登録内容の一致 | MATCH / MISSING / MISMATCH / CONFLICT / UNKNOWN |
| 登録ターゲットへの到達 | REACHABLE / MISSING / RESOLUTION_ERROR / UNKNOWN |

CONFLICT は通常ファイル・ディレクトリなどが配置先を占有している状態。UNKNOWN は権限やI/O障害等によって判定できない状態。RESOLUTION_ERROR はリンク追跡上限などの解決失敗であり、必ず循環が存在すると断定しない。

配置先とターゲットの両方がない場合は、それぞれ MISSING と記録する。MISMATCH の場合は実際のターゲットとその到達状態も表示する。存在確認ではリンク自体と参照先を分け、壊れたリンクを「配置先がない」と誤判定しない。

明示オプションの名前は `--replace` を提案する。

| 配置先の状態 | 通常の fix | fix --replace |
| --- | --- | --- |
| 登録どおりのリンク | 変更なし | 変更なし |
| リンクがない | 作成。親がない場合は --parents が必要 | 作成。親がない場合は --parents が必要 |
| 違うターゲットのリンク | 報告だけ（確定） | 登録内容で張り直す |
| 通常ファイル・ディレクトリ等がある | 競合を報告 | 競合を報告 |
| 確認できない | エラーを報告 | エラーを報告 |

存在しない target へのリンク作成は許可する（確定）。作成・fix はその状態を報告する。target 本体のファイルやディレクトリを作成・復元することはない。既に登録どおりのリンクがあり target だけが消えている場合、fix はリンクを無意味に張り直さず、ターゲット不在を報告する。

--replace は選択中の管理ファイルに登録されたシンボリックリンクだけを対象にする。通常ファイルを上書きする汎用の --force は初版の候補に含めない。

remove は登録と一致するリンクだけを削除して登録解除する。リンク自体がない場合は登録だけ解除できる。別のターゲットや通常ファイル等に変わっていれば、その項目は変更せず競合を報告する。ターゲット本体は削除しない。

remove --keep-link は登録だけを解除する（機能採用は確定）。このモードでは配置先が消えている・別物に変わっている場合でも、配置先を変更せず登録解除できる。通常の remove の削除条件を満たす必要はない。登録されていない配置先を削除する機能は持たない。

adopt は既存のシンボリックリンクだけを受け入れる。既存の登録と一致すれば変更なし、不一致なら登録を上書きせず報告する。壊れた既存リンクも登録して検査対象にできる案。

繰り返し実行できるようにし、作成を再実行して登録・実リンクとも一致していれば変更なしにする。未登録の既存リンクは adopt で登録する。

## 採用した補助機能と適用範囲

機能の採用は確定。次の適用範囲を詳細仕様として提案する。

| オプション | 使える操作 | 責務 |
| --- | --- | --- |
| --dry-run | 作成 / fix / remove / adopt | 実行時と同じ変更予定を表示し、変更を実行しない |
| --relative | 作成 | 入力ターゲットへの相対参照を生成する |
| --keep-link | remove | 配置先を変更せず登録を解除する |
| --parents | 作成 / fix | リンク配置先の不足する親ディレクトリを作成する |
| --replace（名前は提案） | fix | 登録と異なるターゲットのシンボリックリンクを張り直す |

対象外の操作にオプションを付けた場合は、無視せず引数エラーにする。

### --relative

```sh
slink --relative --parents ~/dotfiles/nvim ~/.config/nvim
```

ホーム配下の通常のディレクトリ構成なら、上の操作で生成する target は `../dotfiles/nvim`。CLIから自動登録する link は絶対パスにする提案なので、例えばホームが `/Users/alice` なら次の項目になる。

```toml
version = 1

[[links]]
link = "/Users/alice/.config/nvim"
target = "../dotfiles/nvim"
```

保存するのは生成した target。`relative = true` や元の入力 target は追加保存しない。fix はこの target をそのまま復元できるので --relative を持たない。adopt も既存の格納文字列を保存する操作であり、表記の変換はしない。

相対パス生成では、リンクの親ディレクトリの実際の位置を考慮する。一方、入力ターゲット側の symlink を辿りきった最終ファイルへ参照を付け替えず、指定された参照経路を保つ提案とする。途中の symlink や存在しない要素に関係する `..` を、意味が変わる形で単純に縮約しない。

--parents と併用して親がまだない場合は、既存の祖先と作成するディレクトリから計画を求め、実行時に親を作った後で確認する。親の位置を権限エラー等で確定できない場合は、その項目の変換・作成を行わず報告する。ターゲットが存在しないこと自体は変換を拒否する理由にしない。

### --parents

- 作るのはリンク配置先の不足する親ディレクトリだけ。ターゲット側には作らない。
- 既存ディレクトリの内容やパーミッションを変更しない。新しいディレクトリは実行ユーザーの umask に従う。
- 親に通常ファイルや壊れたディレクトリリンクがある場合、それを置換しない。既存のディレクトリへ到達できる親 symlink は通常のパス解決に従って扱う。
- 作った親ディレクトリを管理ファイルに登録しない。remove でも自動削除しない。
- --parents は実行時の指定であり、管理ファイルに記録しない。後日、親ごと消えていた場合の復元には `fix --parents` を使う。
- 管理ファイル自身の保存先ディレクトリの初期化は Registry の責務であり、このオプションと分ける。

### --dry-run

管理ファイル、リンク、親ディレクトリを変更しない。新しいロックファイルや一時ファイルも作成しない。登録追加・登録解除・親ディレクトリ作成を含めた予定と、実行できない項目を表示する。

```sh
slink --relative --parents --dry-run ~/dotfiles/nvim ~/.config/nvim
slink fix --parents --replace --dry-run
slink remove --keep-link --dry-run ~/.zshrc
```

dry-run 後にファイルシステムが変わる可能性はあるため、通常実行でも直前の検証を行う。

### 初版に追加しない候補

scan --adopt、独立した forget、doctor、restore、apply は、現時点では追加しない提案。順に adopt、remove --keep-link、check、fix、fix の責務に集約できる。ターゲット不在を許可する方針なので --allow-missing も不要。--json は初版に含めない。作成日時・履歴表示・タグ・監視・ターゲットの移動追跡は、具体的な利用目的が出てから検討する。

## 内部構成（提案）

| 部品 | 責務 |
| --- | --- |
| Registry | ファイル選択、解析、検証、コメントを保つ編集、保存 |
| Discovery | 管理項目や探索範囲から検査対象を集める |
| Inspector | リンク本体、期待ターゲット、実ターゲットを検査する |
| Planner | 登録内容・観測結果・オプションから変更予定を組み立てる |
| Executor | 直前の状態を確認し、予定されたリンク操作と登録更新を実行する |
| CLI / Renderer | 引数、表示、終了コードを扱う |

dry-run は通常実行と同じ Planner を通し、Executor を呼ばない。--relative は入力から保存する target を決める処理、--parents は実行計画にディレクトリ作成を加える処理、--keep-link は登録解除だけを計画する処理として分ける。これらを管理ファイル内の追加状態として重複保存しない。

複数リンクと管理ファイルをまとめた単一の原子的トランザクションは約束しない。項目ごとの結果と未完了操作を区別し、次節の保存・復旧契約を用いる。

## macOSでの実装方針（提案）

### OS・ファイルシステム

- macOSの通常のシンボリックリンクを対象にする。Finderのエイリアスファイルは通常ファイルとして扱う。
- Apple Silicon / Intel の両方をビルド対象にする案。最低OSバージョンは、採用するビルド環境とmacOSでの検証結果から公開前に確定する。
- 初版の主な検証対象はローカルのAPFSとする案。大文字・小文字を区別するAPFSと区別しないAPFSの両方を検証する。APFSにはこれらの形式がある。[Apple：Macで利用できるファイルシステム形式](https://support.apple.com/guide/disk-utility/file-system-formats-dsku19ed921c/mac)
- パスの保存表記は保つ。AppleのAPFS資料でも、保存される表記と、大文字・小文字やUnicode正規化に関する照合は区別されている。[Apple File System Guide（アーカイブ）](https://developer.apple.com/library/archive/documentation/FileManagement/Conceptual/APFS_Guide/FAQ/FAQ.html)
- 他のファイルシステムを形式名だけで一律に拒否せず、操作に必要なリンク作成・排他的な移動・同期等の機能が使えなければその操作を中止して報告する。安全性を弱める操作への自動切り替えは行わない。ターゲットが別ボリュームにあるだけなら、リンク配置先の対応可否と分けて扱う。
- 権限やmacOSのアクセス制御で確認できない場所は UNKNOWN / アクセスエラーとし、存在しないパスに分類しない。

### 配置先の識別と入力検証

1. CLIの配置先には実行時のディレクトリ、管理ファイルの相対配置先には選択した管理ファイルのディレクトリを結び付ける。対象を識別するためにリンクの最終要素を辿らない。
2. 既存の親ディレクトリはOSのパス解決で特定し、ディレクトリ識別情報と末尾の名前を使う。`lstat` はシンボリックリンク自体の情報を取得できる。[Apple：lstat(2)](https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man2/lstat.2.html)
3. 大文字・小文字やUnicode表記だけが違う入力は、ファイルシステムでの実際の参照先を確かめる。同じinodeという理由だけで、異なる名前のハードリンクまで同じ配置先と断定しない。
4. 対象がまだない場合は、既存の祖先・残りの経路・ボリュームの照合特性から衝突候補を検出する。OSの照合規則に照らして区別を証明できない候補は曖昧な配置先として報告し、自動選択しない。候補検出用の正規化を、保存パスやtargetの書き換えに使わない。
5. --parents による親の作成後も識別を再確認する。最終的なリンク作成は既存の名前を上書きしない操作にし、遅れて判明した衝突も報告する。

初版で管理ファイルに保存するパス・targetは、正しくUTF-8として表現できる文字列を対象にする。表現できない値は置換文字で保存せず、対象を明示して拒否する。scan/check の表示でも制御文字をそのまま端末へ流さず、エスケープする。

空の link / target とNULを拒否する。link の末尾が `/`、`.`、`..` となる指定やルート自体の指定も拒否し、リンク名として扱う最後の要素を明確にする。親経路中の symlink や `..` はOSの解決順序に従う。

### 管理ファイル自体がsymlinkの場合

管理ファイルを読み始める時点で、その参照先の通常ファイルと親を特定する。保存時は参照先を更新し、管理ファイルへのsymlinkを通常ファイルで置き換えない。解決できないsymlinkや循環はエラーにする。

管理ファイル内の相対 link の基準は、--file等で選択した管理ファイルの場所とする。参照先の実ファイルが別ディレクトリにあっても、この基準を切り替えない。参照元・参照先の同一性は保存前に再確認する。

## 保存と途中失敗（提案）

### 同時実行と管理ファイル保存

- 解決した管理ファイルに対応する固定のロックファイルを使い、変更操作を排他する。原子的なファイル置換でロック対象が別inodeに変わらないよう、管理ファイル本体をロックファイルと兼用しない。
- ロックは待ち続けず、使用中ならその旨を報告して終了する。異なる管理ファイル同士は独立して扱い、配置先の競合はOS操作直前にも検査する。
- macOSのflockは協調するプロセス間のロックであり、通常のエディタの書き込みを禁止するものではない。[Apple：flock(2)](https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man2/flock.2.html)
- 管理ファイルの読み込み時の識別情報と内容を保存し、書き込み直前に照合する。検出した変更は競合として扱い、古い内容を強制保存しない。
- 新しいTOML全体を同じファイルシステムの一時ファイルに書き、必要な同期処理を行ってから参照先の通常ファイルを置換する。初期作成は既存の名前を上書きしない。既存ファイルの権限を維持し、新規管理ファイルは0600を基本にする。
- 協調しないプロセスが照合直後にも更新できる競合窓は残る。検出・中断・復旧情報の保持を実装し、すべての外部書き込みとの完全な直列化や、電源断を含む無条件の永続化を約束しない。

### 未完了操作の記録

作成・登録の組み合わせ、リンク削除と登録解除の組み合わせ、既存リンクの張り直しには、操作中だけ保持する復旧記録を使う案。

復旧記録には、管理ファイルの更新前後を照合する情報、対象リンク、観測した旧状態、意図する新状態、操作の進行段階、退避場所を保存する。管理ファイルの参照先に隣接した専用ファイルとし、新規作成は排他的に行う。通常の編集対象にはせず、操作完了後に削除する。

この記録は未完了操作を判別するためのもの。管理ファイルから手で削除された過去の項目を記憶したり、将来の fix で削除対象を推測したりする用途には使わない。

| 操作 | 保存・実行の順序 |
| --- | --- |
| 作成と新規登録 | 復旧記録 → 不足する親の作成 → 上書きしないリンク作成 → 登録内容の保存 → 完了処理 |
| remove | 復旧記録 → 既存リンクの検証・退避 → 登録解除の保存 → 検証済み退避リンクの削除 → 完了処理 |
| fix --replace | 復旧記録 → 既存リンクの検証・退避 → 上書きしない新リンク作成 → 検証済み退避リンクの削除 → 完了処理 |
| adopt / remove --keep-link | 管理ファイルだけを検証して保存する |
| 不足リンクだけの fix | 親と配置先を検証して作成する。管理ファイルは変更しない |

退避は対象と同じ親の専用領域で行い、既存の名前を上書きしない。移動された実物が観測したsymlinkと一致することを確認してから削除する。競合で通常ファイル等が移動された場合は削除せず、元の場所が空いているときだけ戻す。元の場所が使われていれば両方を保持して具体的な復旧場所を報告する。再帰削除は使わない。

この張り直し方式では、リンク名が一時的に存在しない期間があり得る。初版では張り直しの無停止性を保証しない。

### 再実行と結果表示

| 中断状態 | 次の動作 |
| --- | --- |
| リンク作成済み・登録保存前 | 同じ作成操作の再実行で、実物を照合して登録を完了する |
| removeの退避済み・登録解除前 | 同じremoveの再実行で、記録と実物を照合して続行する |
| 登録解除済み・退避リンクの後片付け前 | 同じremoveの再実行で、確認済みの退避リンクを片付ける |
| 張り直し途中 | 同じ明示的なfix操作の再実行で、旧状態・新状態・退避先を照合して続行する |
| 管理ファイルや実物が記録と合わない | 自動継続を中止し、差分と復旧場所を報告する |

再実行では保存済みの復旧段階を盲信せず、現在のファイルシステムと管理ファイルも観測する。実行が完了した直後、段階の保存前にプロセスが停止する場合も扱う。

未完了記録がある場合、check はその存在を報告して非0を返し、変更はしない。対象・操作・変更権限が異なる新しい変更コマンドは、まず未完了操作を解消する必要がある旨を報告して停止する。通常の fix が未完了の remove を打ち消してリンクを作り直したり、--replace のない実行で張り直しを再開したりしない。

独立した複数項目の操作は順に処理し、成功分を保持する。単純な競合などで未完了状態を残していなければ他の項目を処理できるが、復旧記録を残す失敗ではそれ以降の変更を止める。管理するリンクの親子関係や参照関係に依存がある場合も、単純に別項目とみなさず、その実行で安全に処理順を確定できなければ事前に報告する。

## 終了コード（提案）

| コード | 意味 |
| --- | --- |
| 0 | 要求された操作が完了。check の場合は対象がすべて正常 |
| 1 | check で問題検出、項目単位の失敗・競合・未修復、または scan の部分的な探索失敗 |
| 2 | 引数・管理ファイルの不正など、コマンド全体を開始できないエラー |

作成・fix はリンクを登録どおりに作成・復元する操作。ターゲット不在だけなら状態を報告し、リンク操作自体が成功すれば 0 にする。同じ状態でも check は到達不能を検出したため 1 にする。fix で不一致を発見して --replace がなく未修復となった場合は 1。scan は壊れたリンクを発見したことだけでは失敗にしない。

dry-run は変更予定を正常に作れれば 0、変更が必要だが条件上実行できない項目があれば 1、入力全体の不正なら 2。dry-run の 0 は後続実行の成功保証ではない。

## 実装言語と検証（提案）

第一候補は Rust。TOMLの編集には toml_edit を候補とする。同ライブラリはコメント・空白・項目の相対的な順序を保った編集を提供しているが、dotted keysの順序などに制約がある。ライブラリを採用しただけで、slinkの書式保持要件を満たしたことにはしない。[toml_edit ドキュメント](https://docs.rs/toml_edit/latest/toml_edit/)

`version` と `[[links]]` の `link` / `target` だけを扱う小さなスキーマを維持し、変更する項目だけを編集する。無変更の読み書き、別項目の変更、項目の追加・削除、コメント・空行・引用符・改行形式の保持を検証する。削除する項目と隣接コメントの帰属を区別し、無関係なコメントを消さない。

配布は単一実行ファイルを基本案にする。公開先・Homebrew等の配布方法・最低対応OSの確定は、実装とリリース準備の判断として残す。

macOSで実装時に検証する主な受け入れ条件：

1. 作成とadoptの区別、同じ操作の再実行、壊れたリンクの登録。
2. target不在を許可した作成・fixと、checkの検出・終了コード。
3. 通常のfixと--replaceの境界、通常ファイル・ディレクトリが競合したときの保全。
4. removeと--keep-link、管理ファイルの手編集による解除、linkフィールドの変更後に旧リンクが残ること。
5. --relativeの変換、targetがsymlinkの場合、途中のsymlinkと `..`、存在しないtarget。
6. --parentsによる親の作成、既存親の保全、remove後に親が残ること。
7. 全変更系コマンドのdry-runで管理ファイル・リンク・ディレクトリ・内部補助ファイルが変化しないこと。
8. APFSの大文字・小文字を区別する形式と区別しない形式、Unicode表記、空白・引用符を含むパス。
9. 管理ファイル自体がsymlinkの場合と、同時編集・同時実行。
10. 操作の各段階での失敗・プロセス中断・再実行と、通常のfixによる削除の取り消しを防ぐこと。

本書ではTOML例とMarkdown表の構造を確認した。上記の動作検証はslink実装後にmacOS上で行うものであり、現在の設計書更新によって通過済みと扱わない。
